<ul>

  <% @users.each do |user| %>
    <li>
      <%= user.name %>有<%= Followship.where(following_id: user).size %>個關注者
      <%= render partial: "shared/followship", locals: {user: user} %>
    </li>
  <% end %>

</ul>